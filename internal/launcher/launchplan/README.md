# Launch plan

Converts Git-project discovery into the validated worktree identity, working directory, and ordered bind mounts used
by host container orchestration.

## Non-test files

- `plan.go` — defines, builds, normalizes, and validates the launch filesystem contract.
