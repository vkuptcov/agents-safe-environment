# Terminal detection

Provides Linux terminal predicates shared by the host launcher and the container-side command wrapper.

## Non-test files

- `terminal.go` — detects whether an `io.Reader` is attached to a terminal.
