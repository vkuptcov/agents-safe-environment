# Launcher CLI test doubles

Provides shared fakes and exit errors for tests of the shared CLI package and both product command binaries.

## Non-test files

- `clitest.go` — provides the recording launcher, panic-only dependencies, and exit-code error double.
